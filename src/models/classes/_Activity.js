class Activity {
    constructor(uuid, description, userName, userID) {
        this.uuid = uuid;
        this.description = description;
        this.userName = userName;
        this.userID = userID;
    }
}

export default Activity;